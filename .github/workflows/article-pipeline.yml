name: Article Generation Pipeline

on:
  schedule:
    # 3時間ごとに実行 (0:00, 3:00, 6:00, 9:00 UTC)
    # 日本時間では 9:00, 12:00, 15:00, 18:00
    - cron: '0 */3 * * *'
  workflow_dispatch: # 手動実行も可能

jobs:
  generate-articles:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r automation/requirements.txt
      
      - name: Run pipeline
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          WP_URL: ${{ secrets.WP_URL }}
          WP_USER: ${{ secrets.WP_USER }}
          WP_APP_PASSWORD: ${{ secrets.WP_APP_PASSWORD }}
          # X (Twitter) Credentials
          X_API_KEY: ${{ secrets.X_API_KEY }}
          X_API_SECRET: ${{ secrets.X_API_SECRET }}
          X_ACCESS_TOKEN: ${{ secrets.X_ACCESS_TOKEN }}
          X_ACCESS_TOKEN_SECRET: ${{ secrets.X_ACCESS_TOKEN_SECRET }}
          # Google Cloud / Gemini
          GOOGLE_CLOUD_LOCATION: "global"
        run: |
          cd automation
          python pipeline.py --hours 3 --threshold 75 --limit 1
      
      - name: Upload artifacts on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: pipeline-logs
          path: |
            automation/collected_articles.json
            automation/scored_articles.json
          retention-days: 7
